export enum Player{
    RED = 1,
    YELLOW = 2
}

export enum CellColor {
    NONE,
    RED,
    YELLOW,
}

export enum RunningState{
    RUNNING,
    PAUSED,
    PREGAME,
    PLAYER_ONE_WINS,
    PLAYER_TWO_WINS
}